import chai from 'chai'

const { expect } = chai

describe('Tests', () => {
  it('should run', () => {
    expect(true).to.equal(true)
  })

  it('should do math', () => {
    expect(1+1).to.equal(2)
  })
})
